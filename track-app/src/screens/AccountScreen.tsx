import React from "react";
import { View, Text, StyleSheet } from "react-native";

const AccountScreen = () => {
	return (
		<View>
			<Text style={{ fontSize: 35 }}>AccountScreen</Text>
		</View>
	);
};

export default AccountScreen;